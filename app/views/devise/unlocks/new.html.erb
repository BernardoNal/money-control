<div class="mx-auto w-full max-w-xl py-8">
  <div class="rounded-[2rem] border border-slate-300 bg-white p-8 shadow-sm sm:p-10">
    <p class="text-sm font-semibold uppercase tracking-[0.22em] text-slate-500">Desbloqueio</p>
    <h2 class="mt-3 text-3xl font-semibold text-slate-950">Reenviar desbloqueio</h2>
    <p class="mt-2 text-sm leading-6 text-slate-500">Recupere o acesso a sua conta com um novo e-mail de desbloqueio.</p>

    <div class="mt-8">
      <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div>
          <%= f.label :email, "E-mail", class: "mb-1.5 block text-sm font-medium text-slate-900" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w-full" %>
        </div>

        <div class="mt-8">
          <%= f.submit "Reenviar desbloqueio", class: "inline-flex w-full items-center justify-center rounded-full bg-[#000080] px-5 py-3 text-sm font-semibold text-white transition hover:bg-[#000066] focus:outline-none focus:ring-4 focus:ring-blue-200" %>
        </div>
      <% end %>
    </div>

    <div class="mt-6 border-t border-slate-200 pt-6 text-center">
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
